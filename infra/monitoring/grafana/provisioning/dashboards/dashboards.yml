apiVersion: 1

providers:
  - name: default
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      # JSON dashboards are mounted here (see docker-compose).
      path: /etc/grafana/dashboards
      foldersFromFilesStructure: false
