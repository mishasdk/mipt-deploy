[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "ml-churn"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "scikit-learn",
    "mlflow",
    "lightgbm",
    "optuna"
]

[project.scripts]
churn-pipeline = "ml_churn.pipeline:main"